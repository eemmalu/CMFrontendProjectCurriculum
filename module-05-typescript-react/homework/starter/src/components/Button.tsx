// TODO: Import useState if needed

// TODO: Define ButtonProps interface
// Required props: label (string), onClick (function)
// Optional props: variant ('primary' | 'secondary' | 'success' | 'danger'), disabled (boolean), size ('small' | 'medium' | 'large')

function Button(/* TODO: Add props with proper typing */) {
  // TODO: Implement button component
  // - Should render a <button> element
  // - Apply different styles based on variant prop
  // - Apply different sizes based on size prop
  // - Handle disabled state (opacity, cursor, prevent onClick)
  // - Call onClick handler when clicked (if not disabled)

  return (
    <button>
      {/* TODO: Display label */}
    </button>
  );
}

export default Button;
