// TODO: Define InputProps interface
// Required props: label (string), value (string), onChange (function that takes string)
// Optional props: placeholder (string), type ('text' | 'email' | 'password' | 'number'), error (string)

function Input(/* TODO: Add props with proper typing */) {
  // TODO: Implement input component
  // - Display label above input
  // - Render input with correct type
  // - Set value prop and onChange handler
  // - Add placeholder if provided
  // - Display error message below input in red if provided
  // - Change border color to red if error exists

  return (
    <div>
      {/* TODO: Render label */}
      {/* TODO: Render input */}
      {/* TODO: Render error message if provided */}
    </div>
  );
}

export default Input;
